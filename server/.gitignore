# ── Dependencies ──
node_modules/

# ── Build output ──
dist/

# ── Environment variables — NEVER commit these ──
.env
.env.local
.env.*.local
.env.development
.env.production

# ── Logs ──
logs/
*.log
npm-debug.log*
yarn-debug.log*

# ── TypeScript cache ──
*.tsbuildinfo

# ── Seed / one-off scripts (optional — remove if you want to commit them) ──
scripts/

# ── Editor ──
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# ── OS ──
.DS_Store
Thumbs.db
